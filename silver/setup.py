from setuptools import setup, find_packages

setup(
    name='earthquake_pipeline',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        # Add any additional dependencies here
        # Example: 'requests', 'apache-beam', etc.
    ],
)
